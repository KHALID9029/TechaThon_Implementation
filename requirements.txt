# Pinned by resolving against pyproject.toml on 2026-07-03.
# Re-freeze with `pip freeze > requirements-lock.txt` if you need the full transitive lock.
fastapi==0.139.0
uvicorn[standard]==0.49.0
aiosqlite==0.22.1
pydantic==2.13.4
discord.py==2.7.1
httpx==0.28.1
python-dotenv==1.2.2
tzdata==2025.2; platform_system == "Windows"  # zoneinfo has no IANA db on Windows -- see docs/BACKEND_SETUP.md
pytest==9.1.1
pytest-asyncio==1.4.0
ruff==0.15.20
